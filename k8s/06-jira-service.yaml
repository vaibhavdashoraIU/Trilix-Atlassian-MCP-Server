apiVersion: apps/v1
kind: Deployment
metadata:
  name: jira-service
  namespace: trilix
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jira-service
  template:
    metadata:
      labels:
        app: jira-service
    spec:
      containers:
      - name: jira-service
        image: 996894428841.dkr.ecr.us-east-1.amazonaws.com/trilix/jira-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: trilix-config
        - secretRef:
            name: trilix-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: jira-service
  namespace: trilix
spec:
  selector:
    app: jira-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
